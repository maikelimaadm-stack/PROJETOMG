import { StudioShellPrototype } from "../shell/StudioShellPrototype.jsx";

/** Studio Shell Prototype page — Program 2.1A (mock data only). */
export default function StudioPrototypePage() {
  return <StudioShellPrototype />;
}
