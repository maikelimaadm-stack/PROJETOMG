/**
 * Empresas — metadata de campos / formulário (configuração ModeloBase1).
 */
export {
  buildEmptyEmpresaForm,
  buildEmpFormDefaultConfig,
  EMP_FORM_BASE_PANELS,
  EMP_FORM_DEFAULT_LAYOUT,
  NATIVE_FIELDS as EMP_NATIVE_FIELDS,
  REQUIRED_FIELDS as EMP_REQUIRED_FIELDS,
  UPPER_FIELDS as EMP_UPPER_FIELDS,
  applyDuplicateFieldClears,
  inputClass as empInputClass,
} from "@/modules/empresas/components/formEmp.constants";

export { empresasModuleMetadata } from "@/modules/empresas/config/empresasModuleMetadata.js";
