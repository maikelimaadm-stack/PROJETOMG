# eventscert

Módulo fictício de certificação da **Events Configuration Engine** (Enterprise V18).

Demonstra `onLoad`, `onChange`, `onSave`, `onDelete`, `onFormulaCalculated` e `onValidationCompleted` exclusivamente por metadata.
