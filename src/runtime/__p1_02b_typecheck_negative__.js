/**
 * SENTINELA TEMPORÁRIA — PROVA NEGATIVA DE P1-02B
 *
 * Erro de tipo REAL em arquivo de PRODUÇÃO, dentro do escopo de
 * `jsconfig.typecheck.json`. Existe apenas para provar que o enforcement
 * fail-closed reprova de fato. Este commit é revertido em seguida com
 * `git revert --no-edit` — sem reset, sem amend, sem reescrita de história.
 */
/** @type {number} */
export const P1_02B_TYPECHECK_NEGATIVE = "P1_02B_TYPECHECK_NEGATIVE";
