export const buildMakPreferencesBootstrapQueryKey = (moduleId, clienteId, userId) => [
  "mak-preferences-bootstrap",
  moduleId,
  clienteId,
  userId,
];

export default buildMakPreferencesBootstrapQueryKey;
