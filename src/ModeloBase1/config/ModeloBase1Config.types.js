/**
 * Contrato declarativo do ModeloBase1 (documentação JSDoc — sem runtime TS).
 *
 * @typedef {object} ModeloBase1Config
 * @property {string} moduleId
 * @property {object} makModule — runtime MAK (defineMakModule)
 * @property {object} moduleDefinition
 * @property {string} [scopeCssClass]
 * @property {string} [metricsCounterKey]
 * @property {string} [dropdownQueryKeyPrefix]
 * @property {string} [tableKey]
 * @property {object} components
 * @property {object} hooks
 * @property {object} helpers
 * @property {object} [export]
 * @property {object} [data]
 * @property {object} [labels]
 */

export {};
