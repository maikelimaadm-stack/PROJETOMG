/** MDP-1 platform definition version stub (MDP-5 placeholder). */
export const MDP_PLATFORM_VERSION_ID = "mdp-platform-v1";

export const MDP_ENTITY_IDS = {
  empresas: "mdp-entity-empresas",
  cadcps: "mdp-entity-cadcps",
};

export const DEFAULT_LOCALE = "pt-BR";

export const DEFAULT_CAPABILITIES = {
  layout: true,
  field: true,
  validation: true,
  formula: true,
  events: true,
  actions: true,
  workflow: true,
  custom_fields: true,
  attachments: true,
};
