# Next Slice Spec

If this slice is PASS, the recommended successor is:

**POST-FOUNDATION C — STUDIO DEV PREVIEW RUNTIME UI CONTRACT**

It would consume this isolated runtime's virtual preview frame + verification to define the CONTRACT
of a future dev-preview UI — still WITHOUT real routes, real menus, real modules, backend/Prisma, or
production. It would remain dev-only and gated.

Entry marker exposed by this slice: `compatibility.status =
ready_for_future_dev_preview_runtime_ui_contract_when_explicitly_authorized`, with
`readyForDevPreviewRuntimeUI: false` and `readyForRouteMenuIntegration: false` until an explicit,
separately-approved slice.

Remaining permanently blocked until such a slice: real UI/React/JSX/TSX/DOM/CSS, real routes/menus,
module generation, backend/Prisma/migration, mutation, persistence, real data, production/staging,
and any rewrite of Empresas.
