# Interaction Binding Contract

`createRuntimeUiInteractionBindingContract()` describes interactions as metadata. Each is
`blockedNow: true` and `requiresFutureRuntimeImplementation: true`; `handlerCreated: false`,
`mutationAllowed: false`. `anyHandlerCreated` and `anyMutationAllowed` are `false`.
