# Layout Contract

`createRuntimeUiLayoutContract({ frameMapping })` describes `layoutId`, `layoutKind`, `density`,
`regions`, `ordering`, `responsivePlanMetadata` as metadata. No real CSS, no required Tailwind, no
DOM (`realCss/requiredTailwind/dom: false`).
