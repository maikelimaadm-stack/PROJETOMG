# Theme Projection Contract

`createRuntimeUiThemeProjectionContract()` declares abstract theme token groups (density, spacing,
radius, surface, text, border, semanticStatus) as metadata. No real CSS, no required Tailwind, no
stylesheet, no runtime CSS (`realCss/requiredTailwind/stylesheetCreated/cssRuntime: false`).
