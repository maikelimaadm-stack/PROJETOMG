# Render Boundary Contract

`createRuntimeUiRenderBoundaryContract()` blocks real render: `renderAllowed: false`,
`realRenderProduced: false`, `virtualRenderContractProduced: true`,
`reactElementProduced/domProduced/cssProduced: false`, reason `requires future explicit UI runtime
implementation slice`.
