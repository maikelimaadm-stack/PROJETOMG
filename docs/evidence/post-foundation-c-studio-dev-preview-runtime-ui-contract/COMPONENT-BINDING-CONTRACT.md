# Component Binding Contract

`createRuntimeUiComponentBindingContract({ frameMapping })` maps each contractual placeholder to a
future visual component kind — as metadata. `bindingAllowed: false`, `realComponentPath: null`, and
`reactComponent/jsx/tsx/dom` are all `false`. `anyBindingAllowed` and `anyRealComponentPath` are
`false`. No binding is executed.
