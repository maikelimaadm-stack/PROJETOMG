# Manifest / Compatibility (corrected — exact comparisons)

> Builder: `studio-bridge-decision-core-envelope-builder@1.0.0`.

O compatibility verifier é **fail-closed** e compara EXATAMENTE (sets/ordens/valores — não apenas contagens):
source fields + required/security/eligibility · core allowlist · envelope fields + invariantes · **ordem** do pipeline ·
os 40 issue codes · a shape exata do issue · as 9 dimensões de recurso **e seus valores** · target fields/required/
security/versions/invariants · ARCHITECTURE 1 · owner `consumer_runtime` · superfície da API pública (`['build']`).

`exactComparisonsPerformed: true`. A readiness só marca `readyForEnterpriseBuilderAudit` quando essas provas passam
(`compatibilityOk`). Manifest determinístico, não-criptográfico.

---
_Evidência do slice Post-Foundation C — Studio Bridge Decision Core Envelope Builder. Memória = repositório._
