# Draft Runtime Plan

`createDraftRuntimePlan()` — the future runtime operates ONLY in memory on synthetic data.

`draftRuntimePlanned:true`, `draftRuntimeImplemented:false`, `ephemeralDraftsOnly:true`,
`inMemoryOnly:true`, `syntheticDataOnly:true`, `canonicalDraftsAllowed:false`,
`persistentDraftsAllowed:false`, `sideEffectsAllowed:false`, `filesystemWritesAllowed:false`,
`moduleWritesAllowed:false`.
