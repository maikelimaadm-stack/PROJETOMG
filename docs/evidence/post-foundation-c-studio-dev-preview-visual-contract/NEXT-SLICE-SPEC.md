# Next Slice Spec

If this slice is PASS, the recommended successor is:

**POST-FOUNDATION C — STUDIO DEV PREVIEW RUNTIME SHELL CONTRACT**

It would consume this visual contract's tree/screen/placeholder/state/interaction/theme
metadata to define a still-headless RUNTIME SHELL contract — describing how a controlled dev
preview runtime WOULD mount — WITHOUT creating real routes, real menus, real modules, or any
backend/Prisma surface, and WITHOUT authorizing production.

Entry marker exposed by this slice: `compatibility.status =
ready_for_future_dev_preview_visual_runtime_contract`, with
`readyForDevPreviewVisualRuntime: false` until an explicit, separately-approved slice.

Remaining permanently blocked until such a slice: real UI/React/JSX/TSX/DOM/CSS, real
routes/menus, module generation, backend/Prisma/migration, mutation, persistence,
production/staging, and any rewrite of Empresas.
