# Theme Token Contract

`createDevPreviewVisualThemeTokenContract()` declares 7 token groups (density, spacing, radius,
surface, text, border, semanticStatus) as abstract contractual tokens — NOT real CSS, NOT
required Tailwind classes, NOT a stylesheet. `realCss`, `requiredTailwindClasses`,
`stylesheetCreated`, `cssRuntime` are all `false`.
