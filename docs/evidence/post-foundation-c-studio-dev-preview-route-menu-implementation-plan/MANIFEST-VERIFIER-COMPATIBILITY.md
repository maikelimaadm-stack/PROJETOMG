# Manifest / Verifier / Compatibility — Studio Dev Preview Route/Menu Implementation Plan

Part of the HEADLESS, PLAN-ONLY Studio Dev Preview Route/Menu Implementation Plan. Manifest holds deterministic digests for every part. Verifier detects routeImplemented/menuImplemented true, App/router/navigation/sidebar wiring, runtime UI mount, router primitives, ReactDOM/createRoot/window/document, deep-link implementation, backend/Prisma, fetch/storage, production/staging, real data read/write, prototype relink, missing manual gate and forbidden-flag inversion. Compatibility never authorizes a real implementation slice, App integration, module generation or production.

This document describes **planned metadata only** — it implements nothing. No real route/menu/
router; no App/router/navigation/sidebar wiring; no router primitives; no runtime UI mount
(no ReactDOM/createRoot/window/document); no deep link; no module; no backend/Prisma; no Empresas;
no src/modules; no production/staging; no mutation; no real data; the old Studio prototype is NOT
imported or relinked. Reversible by non-consumption.
