# B-IDENTITY Traceability

B-IDENTITY was CLOSED_BY_CONTRACT in PR #488: the real `bridgeDecisionDigest` covers the full 32-field decision preimage (including the entire targetDescriptor). This plan carries that closure forward: `bIdentityClosedByContract: true`.
