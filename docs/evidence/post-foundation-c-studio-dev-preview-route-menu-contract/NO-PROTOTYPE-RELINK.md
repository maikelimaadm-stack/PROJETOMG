# No Prototype Relink — Studio Dev Preview Route/Menu Contract

CRITICAL: the old Studio prototype is NOT imported, relinked, copied, moved or reused. This
subtree imports nothing from `src/studio/components/`, `src/studio/shell/`,
`src/studio/designers/`, `src/studio/pages/`, `src/studio/navigation/`, `src/studio/dock/`,
`src/studio/panels/`, `src/studio/editor/`, `src/components/`, `src/pages/`, or `src/App.jsx`.
The route isolation contract asserts `noPrototypeRelink: true` and the verifier flags any
prototype relink. Everything here is deterministic metadata, reversible by non-consumption.
