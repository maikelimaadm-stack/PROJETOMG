# Screen Schema

`createDevPreviewScreenSchema({ sandbox })` declares `list` / `form` / `detail` screens
(detail read-only) and the required states `empty` / `loading` / `error` / `blocked`.
Everything is metadata; `componentCreated` and `routeCreated` are `false`.
