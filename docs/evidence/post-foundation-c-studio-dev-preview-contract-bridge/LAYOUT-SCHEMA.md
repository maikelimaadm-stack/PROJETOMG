# Layout Schema

`createDevPreviewLayoutSchema({ sandbox })` describes a 12-column grid, section order,
positions (table/form/detail) and a responsive plan as metadata only.
`realCss`, `requiredTailwindClasses`, `domLayout` are all `false`.
