# No Runtime / Builder / UI / App

Every implementation flag false. Contract-only: no builder, verifier runtime, consumer runtime, digest recompute runtime, UI/App/mount/route/menu/storage/backend/Prisma/module/certification/product.
