# RUNTIME BINDING HARDENING MATRIX

`createStudioRuntimeBindingHardeningMatrix()` — 14 cenários; `allMatched: true`.

## Válidos (referência)

cadastro → ModeloBase1 · operacional → ModeloBase2 (experimental) ·
Empresas certified contract → seed model read-only · cadcps → Field Builder reference ·
Generic Model Runtime → kernel.

## Bloqueados

binding ativa produção · registra módulo · altera App/menu · acessa Prisma direto ·
acessa backend direto · ignora tenant · ignora permission · reescreve Empresas ·
transforma Fuel em produção · usa ModeloBase2 como produção.
