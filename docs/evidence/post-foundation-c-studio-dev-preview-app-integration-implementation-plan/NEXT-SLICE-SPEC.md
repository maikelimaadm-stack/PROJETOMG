# Next Slice — Recommendation

## Required next step

**FABLE 5 — PRE-APP INTEGRATION IMPLEMENTATION ENTERPRISE CHECKPOINT**

Before any real touch of the App, `productionUiGuard`, the router, or a Runtime UI mount, an explicit
enterprise checkpoint (`pre_app_integration_implementation_enterprise_checkpoint`) must be granted.
This plan authorizes **nothing** real on its own.

## After the checkpoint

**POST-FOUNDATION C — STUDIO DEV PREVIEW APP INTEGRATION IMPLEMENTATION SLICE**

The first controlled slice that actually attaches the isolated dev-preview host to the App — behind
the dev-only flag, default-off, fail-closed — following the phases, boundaries, and gates this plan
defines.

## Guardrails carried forward

- 1 slice = 1 PR; dev-only; default-off; fail-closed.
- No prototype relink; no new dependencies; no `.jsx`/`.tsx`/`.css` in the plan layer.
- Governance registry entries stay specific to the slice — never a broad wildcard.
